rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Photos: workers upload to their own folder; admins read all
    match /photos/{userId}/{allPaths=**} {
      allow write: if request.auth != null && request.auth.uid == userId;
      allow read: if request.auth != null;
    }
  }
}
